apiVersion: v2
name: monitoring
description: Grafana + extras for the OpenShift observability stack
type: application
version: 0.2.0

dependencies:
  - name: grafana
    version: "8.*"
    repository: "https://grafana.github.io/helm-charts"
