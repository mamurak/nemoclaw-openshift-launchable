{{- if .Values.eventExporter.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: event-exporter-cfg
  namespace: {{ .Release.Namespace }}
data:
  config.yaml: |
    logLevel: error
    logFormat: json
    route:
      routes:
        - match:
            - receiver: loki
    receivers:
      - name: loki
        loki:
          url: {{ .Values.endpoints.loki.url }}{{ .Values.endpoints.loki.infrastructurePath }}/loki/api/v1/push
          streamLabels:
            job: kubernetes-event-exporter
          headers:
            Authorization: "${MONITORING_TOKEN}"
          tlsConfig:
            insecureSkipVerify: true
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-exporter
  namespace: {{ .Release.Namespace }}
  labels: { app: event-exporter }
spec:
  replicas: 1
  selector:
    matchLabels: { app: event-exporter }
  template:
    metadata:
      labels: { app: event-exporter }
    spec:
      serviceAccountName: monitoring-reader
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      initContainers:
        - name: inject-token
          image: registry.access.redhat.com/ubi9-minimal:9.8-1788166357
          command: ["/bin/sh", "-c"]
          args:
            - |
              TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
              sed "s|\${MONITORING_TOKEN}|Bearer ${TOKEN}|g" /template/config.yaml > /rendered/config.yaml
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - name: cfg-template
              mountPath: /template
            - name: cfg-rendered
              mountPath: /rendered
      containers:
        - name: event-exporter
          image: ghcr.io/resmoio/kubernetes-event-exporter:v1.7
          args: ["-conf=/data/config.yaml"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          resources:
            requests: { cpu: 10m, memory: 32Mi }
            limits: { cpu: 100m, memory: 128Mi }
          volumeMounts:
            - name: cfg-rendered
              mountPath: /data
      volumes:
        - name: cfg-template
          configMap: { name: event-exporter-cfg }
        - name: cfg-rendered
          emptyDir: {}
{{- end }}
