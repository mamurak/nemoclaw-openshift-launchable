The Helm chart deployed everything onto a real **OpenShift cluster** — so you can spend your time on the agent, not on infrastructure. This lesson shows what's running and lets you poke at it live.

## What's underneath

The cluster is **Red Hat OpenShift** — enterprise Kubernetes with Routes, SecurityContextConstraints, and the `oc` CLI built in. The Helm chart installed the OpenShell gateway and its dependencies into this cluster; your agents will run here as governed pods.

```text
OpenShift cluster
├── openshell namespace     ← gateway + agent sandboxes
├── monitoring namespace    ← Prometheus, Grafana, Loki, Tempo
└── your agent (Part IV)    ← the sandbox you create
```

## Inspect it live

Your lab shell already has `oc` pointed at the cluster. Confirm the nodes are up:

```bash
oc get nodes
```

`Ready` nodes = your cluster. Now look at what's already deployed for you:

```bash
oc get ns
oc -n openshell get pods
```

You'll see the `openshell` namespace with the **gateway** (`openshell-0`) running — that's the agent control plane the rest of the workshop uses. You might also see an agent pod like `shifty` if one's already been created; if not, *you* create yours hands-on in **Part IV**.

It really is OpenShift — check an OpenShift-only API:

```bash
oc get scc --no-headers | head -5
oc api-resources | grep -iE "route.openshift.io|security.openshift.io" | head
```

`SecurityContextConstraints` and `route.openshift.io` are OpenShift extensions a vanilla Kubernetes cluster doesn't have. That's the Red Hat layer your agents run on.

Check the Routes the Helm chart created — these are how you reach the gateway and console from your browser:

```bash
oc get routes -A
```

<Callout type="tip" title="Routes, not NodePorts">
OpenShift Routes give each service a real hostname with TLS termination — no port-forwarding hacks needed. The gateway, console, and Grafana are all reachable at their Route URLs.
</Callout>

<Callout type="rh" title="The base is done">
A real OpenShift cluster, pre-built and Ready. Everything from here up — the gateway, the agent — sits on this. Next: the OpenShell gateway that turns this cluster into an agent platform.
</Callout>
